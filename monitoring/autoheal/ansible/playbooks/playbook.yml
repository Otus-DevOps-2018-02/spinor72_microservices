---
- name: Start node
  hosts: all
  become: true
  gather_facts: False

  tasks:
    - name: Restart a container
      docker_container:
        name: "reddit_{{ alert.labels.job }}_1"
        state: started
        restart: yes
