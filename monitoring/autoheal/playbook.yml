---
- name: Start node
  hosts: all
  become: true
  gather_facts: False

  tasks:
    - name: Restart a container
      docker_container:
        name: "{{ node }}"
        # image: someuser/appimage
        state: started
        restart: yes
